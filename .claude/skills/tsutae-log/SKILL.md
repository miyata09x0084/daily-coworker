---
name: tsutae-log
description: ALWAYS invoke when the user asks about their logged one-on-one exchanges or follow-ups. 「つたえログ」「やりとりの記録」「今週のふりかえり」「渡すカードを作って」「相手に伝えること」「約束どうなってる」を検知したら、記憶や推測で答えず必ずこのスキルを起動する。webapp/ のローカルAPIから実データを読む手順と、少ない件数で断定しない規律が必須のため。
---

# つたえログ

`webapp/`（つたえログ）に貯まっている実データを読み、ふりかえり・次の一手の提案・
記録の追加を行う。**会話の記憶から答えない。必ず実データを読む。**

## 前提

- データの実体: `data/tsutae-log/db.json`（git 管理外の個人データ）
- 集計・警告・カード文面はすべてアプリ側（`webapp/server/insights.js` / `handoff.js`）が出す。
  ここで数え直さない。数える場所を増やすと、画面とAIの言うことがずれる。

## このデータの読み方（いちばん大事）

このログが記録しているのは**相手のことではなく、ユーザー（伝えた側）の伝え方**。
未完了が増えていても、それは相手の怠慢ではなく、伝え方が合っていないサイン。

- 相手を主語にした指摘を書かない（「できていない」「動いていない」「忘れている」）
- 指摘は必ずユーザーの行動に向ける（「一度に4件渡していた」「口頭だけだった」）
- 催促を提案しない。回数を増やす案は出さない
- 「なんで」で始まる問いを提案しない

## 1. データの取り方

まずサーバーが動いているか確かめる。

```bash
curl -s --max-time 2 http://127.0.0.1:4173/api/insights > /dev/null && echo up || echo down
```

- **up** → API を使う
  - ふりかえり: `curl -s "http://127.0.0.1:4173/api/review?format=markdown"`
  - 集計: `curl -s http://127.0.0.1:4173/api/insights`
  - 渡すカード: `curl -s "http://127.0.0.1:4173/api/handoff?nextTalkDate=YYYY-MM-DD"`
- **down** → 起動してよいか一言ことわってから `cd webapp && npm start`（バックグラウンド）。
  起動を断られた場合のみ `data/tsutae-log/db.json` を直接読む。その場合は
  「警告と集計はアプリを起動していないため自分で数えた概算」と明示する。

データが空（記録0件）なら、分析を作らずにそう言う。ないものから傾向は出ない。

## 2. ふりかえりを求められたとき

1. `/api/review?format=markdown` の出力をそのまま土台にする（数字を書き換えない）
2. そのうえで、アプリが出せない**解釈**だけを足す:
   - やりとりの本文（`summary` / `note`）を読んで気づいたこと
   - 様子が重かった時期に、こちらが何件渡していたかの対応
   - 同じ用件が何度も出てきていないか（＝伝え方を変える合図）
3. 最後に「次の一手」を **1つだけ** 提案する。複数出すと実行されない。
   提案は必ず「次はこう伝えてみる」の形にする。

### 断定しない線引き

- `techniqueEffect.rows` の `enough: false` の行は**根拠として使わない**。
  件数が足りないことをそのまま伝える。
- その日の様子（condition）は記録者から見た印象であって、診断でも測定値でもない。
  相手の状態の断定として書かない。
- 医療・福祉・人事の判断（服薬、診断、支援区分、評価など）には踏み込まない。
  気になる兆候は「専門の人に相談する材料」として出す。

## 3. 記録の追加を頼まれたとき

会話から埋められる項目だけを埋め、**推測で埋めない**。
特に `condition`（その日の様子）と `understanding`（理解確認）は、
ユーザーが言っていないなら入れない（既定のまま送る）。

```bash
curl -s -X POST http://127.0.0.1:4173/api/interactions \
  -H 'Content-Type: application/json' \
  -d '{
    "summary": "（会話から取った本文）",
    "channel": "face",
    "commitments": [
      { "title": "（決まったこと）", "owner": "partner", "due": "YYYY-MM-DD" }
    ]
  }'
```

- `owner`: `partner`（相手） / `me`（自分） / `both`（いっしょに）
- 期限が会話に出ていなければ `due` を入れず、**「いつまでにするか決めますか？」と聞く**。
  日付のない依頼は実行されない、というのがこのアプリの前提。
- 送ったあと、作成された内容をユーザーに読み上げて確認を取る。

## 4. 渡すカードを頼まれたとき

`/api/handoff` の `text` を**そのまま**提示する。整えたり要約したりしない。
一度に渡す量と語彙は意図して抑えてあり、過ぎた日付は載せない設計になっている。

`note` に「載せきれなかった件数」が入っていたら、それも併せて伝える。

## 出力の形

- 数字は API の出力をそのまま使う
- 提案は1つ
- 200〜400字程度。長いふりかえりは読まれない
